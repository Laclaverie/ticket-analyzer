# Database

This directory hosts database assets.

- migrations: ordered schema migrations.
- seeds: deterministic seed data for local and test runs.
- schema-docs: data model and evolution notes.

Current baseline: SQLite first, PostgreSQL-ready schema practices.
