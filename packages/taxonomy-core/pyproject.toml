[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "taxonomy-core"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["domain-models"]

[tool.hatch.build.targets.wheel]
packages = ["src/taxonomy_core"]

[tool.uv.sources]
domain-models = { workspace = true }

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
