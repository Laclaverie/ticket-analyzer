[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "analytics-core"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "sqlalchemy>=2.0",
    "persistence",
]

[tool.hatch.build.targets.wheel]
packages = ["src/analytics_core"]

[tool.uv.sources]
persistence = { workspace = true }

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
